package pt.tecnico.dsi.ldap

import org.scalatest.BeforeAndAfter

import scala.util.{Failure, Success}

class DeleteSpec extends UnitSpec with BeforeAndAfter {

//  before {
//    simpleLdap.addEntry(s"name=john,${settings.baseDomain}", Map("name" -> "john"))
//  }
//
//  "deleteEntry" should "succeed" in {
//    simpleLdap.deleteEntry(s"name=john,${settings.baseDomain}") onComplete {
//      case Failure(cause) => fail(cause)
//      case Success(result) =>
////        simpleLdap.search(settings.baseDomain, "name") onComplete {
////          case Failure(cause) =
////        }
//    }
//
//
//  }
}
